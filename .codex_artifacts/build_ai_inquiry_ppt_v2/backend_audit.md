# 后端代码事实审查（用于 13 页中文成果 PPT）

审查日期：2026-09-08。范围：只读检查 README、PROJECT_STRUCTURE、后端实现、测试源码及《平台未来发展方向与测试规划.md》。已应用 karpathy-guidelines。没有修改应用代码，没有调用真实模型，没有读取运行凭证，没有运行测试。以下“已实现”仅指存在对应代码路径，不等于本次实测通过、生产可用或教学成效已证实。路径相对项目根目录，冒号后为可定位行号。

## 可直接采用的事实矩阵

| 主题 | PPT 可采用表述 | 源码依据 | 必要边界 |
| --- | --- | --- | --- |
| 平台定位 | 面向教师的探究式教学设计工作台，以阶段对话、专家咨询、草案审阅和导出组织备课过程 | `app/api/sessions.py:104`、`app/api/chat.py:283`、`app/api/export.py:17` | 现有主体是教师备课；学校、班级、学生过程管理属于规划 |
| 七阶段 | 观察起点 → 循疑问题 → 可能的猜想 → 实验设计 → 实验中的新问题 → 可能的结论 → 延伸与新问题 | `app/workflow/flows.py:132`、`:139`、`:144`、`:149`、`:154`、`:159`、`:164`、`:169` | 保留代码中的“可能的”及“新问题”，不擅自改为线性标准答案链 |
| 流程模板 | 已配置七阶段、三步快速探究、STEAM 项目化及昆虫旅馆四种流程 | `app/workflow/flows.py:132`、`:174`、`:196`、`:223` | 会话创建时选择流程；已有会话不能切换流程，接口固定返回 409，见 `app/api/sessions.py:254` |
| 主导师 | 一个主导师贯穿流程，当前阶段目标动态注入提示词 | `app/agents/config/agents.yaml:2`、`app/services/prompt_service.py:24`、`:30` | 不是每阶段各运行一个专属 Agent；不是后台持续自主运行任务 |
| 五位专家 | 昆虫观察、自然生态、数学数据、安全伦理、物理探究五个领域角色 | `app/agents/config/agents.yaml:16`、`:29`、`:41`、`:55`、`:68` | YAML 注册表与独立角色提示词划分能力，共用模型调用服务；不能宣称五套独立训练模型 |
| 调度 | 教师按需选择一位专家咨询，本轮结束后回到主导师工作方式 | `app/api/chat.py:358`、`:449`、`:487`、`:701`；`app/agents/prompts/main_tutor.md:7` | 显式 `expert_id` 触发，主导师可建议选择；没有自动专家路由、并行专家会诊、同轮主导师二次总结 |
| 上下文 | 主导师与专家共享会话历史、当前草案、此前已定稿阶段和会话参考资料 | `app/api/chat.py:365`、`app/services/context_service.py:22`、`:48`、`:75`、`:103` | 历史有 60,000 字符预算，见 `context_service.py:8`；专家建议进入下一轮共享历史，不等于自动采纳或自动改草案 |
| 会话资料 | 支持 PDF、DOCX、TXT、MD，提取后的可用正文进入会话上下文 | `app/services/session_file_service.py:12`、`:56`；`app/services/context_service.py:103` | 默认单文件 20 MB、每会话 10 个、正文共 50,000 字符，见 `app/core/config.py:20`；扫描 PDF 无 OCR，见 `session_file_service.py:67` |
| 草案何时生成 | 教师启用草案模式后，可根据本轮要求生成阶段草案；首次草案直接写入 | `app/api/sessions.py:123`、`:311`；`app/api/chat.py:428`、`:487`、`:514`、`:539` | 默认关闭；普通引导、专家咨询不会自动更新草案。首次空草案与后续修订的审阅机制不同 |
| 草案修订与审阅 | 已有草案可选中局部交给 AI 修订，形成差异提案，教师逐项接受或拒绝 | `app/api/chat.py:578`、`:602`、`:627`、`:653`；`app/services/draft_proposal_service.py:45`、`:159`、`:177`、`:200` | 编辑需有效选区；已有草案整体重生成也形成提案，见 `chat.py:546`。手动保存能力见 `sessions.py:486` |
| 阶段确认 | 教师提交阶段定稿并推进流程，完成后可导出 | `app/api/sessions.py:268`、`:290`、`:298`；`app/api/chat.py:243` | 阶段推进是教师请求触发，不是模型自行判断并完成审批 |
| 轮次回滚 | 可撤销最近 N 轮记录，删除关联消息和 RAG 记录，并恢复受影响阶段的先前草案 | `app/api/sessions.py:413`、`:425`、`:437`、`:456` | 这一路径不回退阶段游标，不恢复全平台历史，也未处理草案提案状态；不能包装为完整版本控制 |
| 返回上阶段 | 可回到上一阶段，撤销该阶段确认并清空定稿，保留原草案和对话 | `app/api/sessions.py:395`、`:407` | 与“轮次回滚”是两个分支；不删除所有后续阶段数据 |
| 导出 | 将各阶段定稿优先、草案次之，汇编为 Markdown 教案 | `app/api/export.py:31`、`:32`、`:34`；`app/services/export_service.py:25`、`:29` | 当前代码不是 Word/PDF/PPT 导出；尚无草案阶段会放占位文字，导出不要求所有阶段已完成 |
| 登录与隔离 | 支持普通用户和管理员入口，会话操作按所属用户检查 | `app/core/auth.py:9`、`:22`；`app/services/session_access_service.py:7`；`app/api/sessions.py:150` | 基础双角色权限，不是学校/组织多租户治理；管理员注册由配置开关控制，见 `app/api/auth.py:53` |
| SSE 与中断 | 对话通过 SSE 推送阶段、角色、文本、状态、草案与完成事件；支持请求取消 | `app/api/chat.py:439`、`:477`、`:498`、`:527`、`:790`、`:813`、`:835`、`:845` | 模型流完成后才写入该轮对话，见 `chat.py:758`、`:774`；不要泛化为“中断回滚所有副作用” |
| 课程库管理 | 管理员可上传、删除、设置专家文件授权、查看检索记录、导入导出及重建向量索引 | `app/api/curriculum.py:87`、`:132`、`:162`、`:180`、`:191`、`:210`、`:250`、`:304` | 普通用户可查看文件列表；新上传文件授权为空，见 `curriculum.py:116`；后台综合运营管理仍待建设 |
| 课程 RAG 的真实入口 | 仅本轮所选专家，在获授权课标文件范围内检索并获得引用上下文 | `app/api/chat.py:373`、`:374`、`:376`、`:459`；`app/services/curriculum_permission_service.py:46` | 主导师和草案不直接查询课程库；二者可从已保存专家回答中间接继承建议。不能画“课程 RAG → 主导师”直连 |
| 检索隔离 | 先查询专家获权来源，再在数据库评分前与 Chroma 查询时过滤 | `app/services/curriculum_knowledge_service.py:330`、`:335`；`app/services/curriculum_vector_service.py:267`、`:272`、`:283` | 文件→专家授权关系，每轮重新读取；不是按学校、教师个人分区的课程知识库 |
| 混合检索 | 本地 BGE Embedding/Chroma 与 BM25 参与融合评分，并加入词覆盖、短语等规则重排 | `app/services/curriculum_vector_service.py:88`、`:159`；`app/services/curriculum_knowledge_service.py:343`、`:413`、`:419` | 向量正常时只保留达到相似度门槛的向量候选，BM25 参与排序，非两路候选无条件并集，见 `curriculum_knowledge_service.py:379`；不是 cross-encoder 学习式重排 |
| 片段溯源 | 回复附课标来源；审计保存专家、授权来源、命中片段及多路分数 | `app/services/rag_service.py:65`、`:91`、`:127`、`:133`；`app/api/chat.py:175`、`:759`；`app/api/curriculum.py:211` | 这是检索来源可追溯，不保证答案每句话被证据支持，更不等于课标符合性自动认证 |
| 检索降级 | 向量不可用或查询失败时可退回 BM25，并记录模式及错误 | `app/services/curriculum_knowledge_service.py:351`、`:373`、`:379`、`:442` | 召回成功但门槛后无候选时返回空，不自动用低相似度 BM25 补齐；检索错误也可返回空上下文，见 `rag_service.py:40` |
| 模型适配 | 使用 OpenAI 兼容 Chat Completions 流式接口，共享模型配置；无密钥或调用异常时有 Mock 回复 | `app/services/llm_service.py:22`、`:27`、`:40`、`:47`、`:52`、`:69` | Mock 不能当真实模型质量；`done.degraded` 目前固定 false，见 `app/api/chat.py:801`，不宜宣称已完整监控降级状态 |

## PPT 最需要规避的表述风险

| 不宜宣称 | 准确替代表述 | 证据与原因 |
| --- | --- | --- |
| “系统自动调度五位专家协作” | “主导师引导，教师按需咨询五类专家” | `app/api/chat.py:360`、`:449`：单一显式专家分支 |
| “RAG 为所有 Agent 提供课标依据” | “课程库按文件授权，给本轮所选专家提供检索参考” | `app/api/chat.py:373`；`PROJECT_STRUCTURE.md:72` |
| “每轮聊天自动形成草案” | “开启草案模式后生成或修订阶段草案” | `app/api/sessions.py:123`；`app/api/chat.py:487` |
| “所有 AI 修改都先审批才落入草案” | “首次草案直接写入，已有草案修订支持差异审阅” | `app/api/chat.py:539`、`:546`、`:653` |
| “支持全量历史版本恢复” | “支持轮次撤销与返回上阶段两种操作” | `app/api/sessions.py:395`、`:413`；提案、文件、所有阶段状态不构成统一快照 |
| “可任意切换教学流程” | “新建会话时选择适合的探究流程” | README 有旧切换示例（`README.md:161`），但实现固定拒绝（`app/api/sessions.py:254`） |
| “已接入传感器开展长期监测” | “昆虫旅馆流程含传感部署教学指引，真实数据接入列入后续规划” | `app/workflow/flows.py:76`、`:244` 只是提示内容；未来文档 `:396`、`:862` 为规划 |
| “具备昆虫生命周期观察平台/学生入口” | “拟以昆虫生命周期长期观察作为下一阶段标杆场景” | `平台未来发展方向与测试规划.md:152`、`:198`、`:220`、`:840`；现有模型表没有学生观察记录结构（`app/db/models.py:14` 至 `:169`） |
| “已通过 20 并发、95% 成功率验收” | “计划以 20 并发、核心流程成功率≥95%、非模型 API P95<1s 为首阶段验收目标” | `平台未来发展方向与测试规划.md:752`、`:756`、`:757`；没有本次压测数据 |
| “已证明减负提效/提升学生探究能力” | “旨在降低备课整理成本，拟在试点中验证教师效率与教学质量” | 当前实现及规划材料不提供对照研究或实测教育成效 |
| “管理员权限已达到完整运营治理要求” | “已实现基础用户隔离与课标管理权限，后续补齐运营后台” | `app/core/auth.py:22`；未来文档 `:250`、`:254`、`:326`；公开管理员注册可按配置启用 |
| “所有中断都完全无落库副作用” | “取消支持停止当前生成，正常完成后才保存该轮对话” | `chat.py:508` 先拒绝旧提案并提交；新提案在 `:563`/`:668` 附近先提交；阶段动作在 `:296` 已执行。不能以 README 的半截回答不落库推断原子事务覆盖一切 |
| “VECTOR_REQUIRED=true 保证聊天绝不降级” | “向量导入可配置为强制要求，查询链路仍需进一步验证和完善错误策略” | 导入检查在 `curriculum_knowledge_service.py:195`；检索异常在 `:373` 被吸收，并在 `rag_service.py:43` 再兜底 |

## 测试证据等级

- 已检查到测试源码覆盖：Agent 与阶段配置（`tests/test_day3.py:274`、`:395`）、登录/管理员（`:474`、`:539`）、用户隔离（`:727`）、BM25/混合检索/授权（`:964`、`:1038`、`:1354`）、中断入口（`:1396`）、首次草案/修订提案（`:1437`、`:1478`）、轮次回滚/阶段返回（`:1538`、`:1655`）、单轮专家（`:1603`）、四类会话文件（`:1691`）。
- 测试文件显式清空模型密钥并关闭真实向量检索，见 `tests/test_day3.py:20`、`:22`；它使用临时数据库，见 `:16`。这些是 Mock/隔离环境测试定义，不能宣称真实模型质量或实际部署性能。
- 本次未执行测试；不填通过率、测试耗时或生产 SLA。已有测试函数名不等于本次通过证明。
- 并发验收目标和长期稳定性测试写在未来文档：5/20/50/100 并发层级见 `平台未来发展方向与测试规划.md:745`；2/8/24 小时 soak test 见 `:581`；验收目标见 `:752`。

## 后续路线图可采用内容（必须标“规划/待验证”）

1. 近期：稳定既有主导师/专家流程，完善课程库权限审计与上传体验，补充关键接口和并发测试，整理昆虫生命周期标杆课程包；依据 `平台未来发展方向与测试规划.md:831`。
2. 中期：后台数据管理、学校/班级/项目管理、学生观察入口、观察时间轴、周报、图片资料及数据表格；依据同文 `:840`。
3. 长期：区域自然教育项目平台、多学校项目协同、教师培训/课堂实施/学生评价衔接、传感器与自然教育资源接入；依据同文 `:853`。
4. 工程配套：SQLite 向 PostgreSQL/MySQL 演进、异步任务队列、缓存限流、可观测性；依据同文 `:768`、`:781`、`:797`、`:811`。当前 SQLite 和进程内中断注册表不能推导规模化多实例并发能力，见 `app/db/database.py:11`、`app/services/chat_interrupt_service.py:23`。

建议技术图主干：教师 → 会话/流程 → 本轮分支〔主导师引导｜草案能力｜手选专家〕→ SSE → 消息/阶段草案/提案 → 教师确认 → Markdown 导出。独立画出“管理员维护课标 → 文件授权 → 获权专家的 BM25+向量检索 → 引用/检索记录”；会话参考资料则连接到三类本轮分支。
